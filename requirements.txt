click>=8.1.0
rich>=13.7.0
pyyaml>=6.0.1

# Dev / test
pytest>=8.2.0
ruff>=0.4.0
jsonschema>=4.20.0   # validates SARIF output against the real official schema in tests

schedule>=1.2.0             # schedule subcommand

# Dashboard tests (fastapi.testclient.TestClient needs httpx2 — starlette
# deprecated plain httpx here; confirmed real via the exact same issue
# surfacing as a hard RuntimeError in a sibling portfolio repo, fixed there
# first, applied proactively here before it does the same)
fastapi>=0.110.0
httpx2>=2.0.0
